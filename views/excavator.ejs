
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Excavator Entities</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        /* Global Styles */
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f3f4f6;
            color: #333;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        h1 {
            font-family: 'Roboto', sans-serif;
            font-size: 36px;
            color: #2c3e50;
            text-align: center;
            margin: 30px 0;
            font-weight: 700;
            letter-spacing: 1px;
        }

        .container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 20px;
            padding: 20px;
            max-width: 1200px;
            margin: 0 auto;
        }

        /* Card Styles */
        .entity {
            background-color: #fff;
            border-radius: 15px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
            padding: 25px;
            transition: transform 0.3s ease, box-shadow 0.3s ease, background-color 0.3s ease;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            overflow: hidden;
        }

        .entity:hover {
            transform: translateY(-10px);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
            background-color: #ecf0f1;
        }

        .entity h2 {
            font-size: 22px;
            color: #2c3e50;
            margin-bottom: 15px;
            font-weight: 600;
        }

        .entity p {
            font-size: 16px;
            color: #555;
            margin-bottom: 12px;
            line-height: 1.6;
            transition: color 0.3s ease;
        }

        .entity p strong {
            color: #3498db;
            font-weight: 500;
        }

        /* Image Section */
        .entity .vehicle-image {
            width: 100%;
            height: 180px;
            background-color: #dcdde1;
            border-radius: 10px;
            margin-bottom: 15px;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .entity .vehicle-image img {
            width: 100%;
            height: auto;
            object-fit: cover;
            border-radius: 10px;
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            font-size: 18px;
            color: #888;
            padding: 50px 20px;
        }

        /* Button Styles */
        .entity .btn {
            padding: 10px 20px;
            background-color: #3498db;
            color: #fff;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-align: center;
            transition: background-color 0.3s ease;
        }

        .entity .btn:hover {
            background-color: #2980b9;
        }

        /* Responsive Adjustments */
        @media (max-width: 768px) {
            h1 {
                font-size: 28px;
            }

            .container {
                grid-template-columns: 1fr;
                gap: 20px;
            }

            .entity {
                padding: 20px;
            }
        }

    </style>
</head>
<body>
    
    <h1><% if (data.length > 0) { %><%= data[0].vehicleType %> Entities<% } else { %>No Vehicle Entities<% } %></h1>


    <div class="container">
        <% if (data && data.length > 0) { %>
            <% data.forEach(function(entry) { %>
                <div class="entity">
                    <!-- Vehicle Image Section -->
                    <div class="vehicle-image">
                        <img src="<%= entry.imageURL || 'https://via.placeholder.com/300x180.png?text=No+Image+Available' %>" alt="Vehicle Image">
                    </div>
                    <h2><%= entry.name %></h2>
                    <p><strong>Vehicle Type:</strong> <%= entry.vehicleType %></p>
                    <p><strong>Vehicle Number:</strong> <%= entry.vehicleNumber %></p>
                    <p><strong>Mobile Number:</strong> <%= entry.mobile %></p>
                    <p><strong>Registration Number:</strong> <%= entry.registrationNumber %></p>
                    <p><strong>License Number:</strong> <%= entry.licenseNumber %></p>
                    <p><strong>Location:</strong> <%= entry.location %></p>
                    <p><strong>Additional Info:</strong> <%= entry.additionalInfo || 'N/A' %></p>
                    
                    <!-- WhatsApp Contact Button -->
                    <a href="https://wa.me/91<%= entry.mobile %>?text=Hi,%20I'm%20interested%20in%20your%20<%= entry.vehicleType %>.<% if (entry.additionalInfo) { %> %20Additional%20info:%20<%= entry.additionalInfo %><% } %>" target="_blank">
                        <button class="btn">Contact Owner</button>
                    </a>
                </div>
            <% }) %>
        <% } else { %>
            <div class="empty-state">
                <p>No vehicles available at the moment. Please check back later.</p>
            </div>
        <% } %>
    </div>
    
    

</body>
</html>
